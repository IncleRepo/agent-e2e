# 프로젝트 워크플로 시나리오

정적 경로는 상위 `routes.json`에 작성한다. 검색, 필터, 상세 이동, 다운로드처럼 행동 검증이 필요한 시나리오는 이 폴더에 `*.spec.ts`로 추가한다.

모든 프로젝트 시나리오는 읽기 전용 안전장치가 적용되도록 다음 모듈에서 `test`와 `expect`를 가져와야 한다.

```ts
import { expect, test } from '../../../core/test';
```

공통 화면 감사가 필요하면 `runPageAudit`을 사용한다.
